---
phase: 04-specialized-agent-nodes
plan: 02
type: execute
wave: 2
depends_on: ["04-01"]
files_modified:
  - agents/nodes/query_planner.py
  - agents/nodes/sql_generator.py
  - tests/agents/test_query_planner.py
  - tests/agents/test_sql_generator.py
autonomous: true
requirements: [AGENT-003, AGENT-004, LLM-003]
must_haves:
  truths:
    - "Query planner generates structured JSON plan with select/from/joins/where/group_by/order_by/limit/ctes keys"
    - "Query planner classifies complexity as simple/moderate/complex"
    - "Query planner gracefully handles malformed LLM JSON output with a safe default plan"
    - "SQL generator produces dialect-specific SQL starting with SELECT or WITH"
    - "SQL generator strips markdown code fences from LLM output"
    - "SQL generator rejects non-SELECT/WITH output with error in state"
    - "SQL generator returns human-readable SQL explanation"
  artifacts:
    - path: "agents/nodes/query_planner.py"
      provides: "CoT JSON plan generation via ChatGroq"
      min_lines: 50
    - path: "agents/nodes/sql_generator.py"
      provides: "Dialect-specific SQL generation with explanation"
      min_lines: 50
    - path: "tests/agents/test_query_planner.py"
      provides: "AGENT-003 unit tests"
      min_lines: 40
    - path: "tests/agents/test_sql_generator.py"
      provides: "AGENT-004 unit tests"
      min_lines: 50
  key_links:
    - from: "agents/nodes/query_planner.py"
      to: "langchain_groq.ChatGroq"
      via: "lazy import + ainvoke"
      pattern: "ChatGroq"
    - from: "agents/nodes/sql_generator.py"
      to: "langchain_groq.ChatGroq"
      via: "lazy import + ainvoke"
      pattern: "ChatGroq"
    - from: "agents/nodes/query_planner.py"
      to: "graph/state.py"
      via: "reads resolved_query, schema, relevant_tables; writes query_plan"
      pattern: "resolved_query.*user_query"
    - from: "agents/nodes/sql_generator.py"
      to: "graph/state.py"
      via: "reads query_plan, db_type, schema; writes generated_sql"
      pattern: "generated_sql"
---

<objective>
Implement the query planner and SQL generator agent nodes with full test coverage.

Purpose: These are the "output side" of the pipeline — the planner creates a structured JSON execution plan from the retrieved schema, and the generator translates that plan into dialect-specific SQL. Together with Plan 01's gatekeeper and schema linker, they complete the 4 core agent nodes for Phase 4.

Output: Working query_planner_node and sql_generator_node, 6+ unit tests all passing, full suite green.
</objective>

<execution_context>
@C:/Users/Chaitanya/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/Chaitanya/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-specialized-agent-nodes/04-CONTEXT.md
@.planning/phases/04-specialized-agent-nodes/04-RESEARCH.md
@.planning/phases/04-specialized-agent-nodes/04-01-SUMMARY.md

<interfaces>
<!-- Key types and contracts from Plan 01 that this plan depends on. -->

From graph/state.py (after Plan 01 modifications):
```python
class AgentState(TypedDict):
    messages: Annotated[list, add_messages]
    user_query: str
    resolved_query: Optional[str]   # NEW — from Plan 01
    db_type: str
    db_manager: Optional[object]
    query_type: Optional[str]
    schema: Optional[dict]
    relevant_tables: Optional[list]
    query_plan: Optional[str]       # planner writes here (JSON string or dict)
    generated_sql: Optional[str]    # generator writes here
    db_results: Optional[list]
    error_log: Optional[str]
    retry_count: int
    final_answer: Optional[str]
```

From tests/agents/conftest.py (created in Plan 01):
```python
def make_agent_state(user_query: str = "test query", db_manager=None) -> dict:
    # Returns complete 14-field AgentState dict
```

Query planner reads: `resolved_query` (or `user_query`), `schema`, `relevant_tables`
Query planner writes: `query_plan` (JSON dict with select/from/joins/where/group_by/order_by/limit/ctes/complexity)

SQL generator reads: `query_plan`, `db_type`, `schema`, `resolved_query` (or `user_query`)
SQL generator writes: `generated_sql`, and optionally an explanation field
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Implement query planner node with tests</name>
  <files>tests/agents/test_query_planner.py, agents/nodes/query_planner.py</files>
  <behavior>
    - test_returns_json_plan: Mock ChatGroq.ainvoke returns JSON with select/from/joins/where/group_by/order_by/limit/ctes/complexity keys. Assert result `query_plan` is a dict with all required keys present.
    - test_malformed_json_fallback: Mock ainvoke returns `"Here is the plan: {invalid json..."`. Assert result `query_plan` is the safe default dict (all empty lists, limit=None, complexity="simple"). No exception raised.
    - test_strips_json_fences: Mock ainvoke returns ````json\n{"select":["*"],...}\n` ``` `. Assert plan parsed correctly despite markdown fences.
    - test_uses_resolved_query: State has both `resolved_query` and `user_query`. Assert the HumanMessage content sent to LLM contains the resolved_query value (inspect mock_llm.ainvoke call args).
    - test_prompt_constant_exists: Assert `_PLANNER_PROMPT` (or similar) is a non-empty string (LLM-003).
  </behavior>
  <action>
**Tests first (RED):** Create `tests/agents/test_query_planner.py` with 5 tests. Use `patch("agents.nodes.query_planner.ChatGroq")`, `AsyncMock`, `asyncio.run()`. Import `make_agent_state` from `tests.agents.conftest`.

**Implementation (GREEN):** Replace `agents/nodes/query_planner.py` placeholder:
- Module-level `_PLANNER_PROMPT` constant (LLM-003). Prompt instructs LLM to generate a JSON query plan with explicit fields: `select`, `from`, `joins` (each with `table`, `type`, `on`), `where`, `group_by`, `order_by`, `limit`, `ctes` (each with `name`, `query`), `complexity` (simple/moderate/complex). Include schema context and relevant_tables in the prompt. Instruct LLM to respond with ONLY valid JSON.
- Module-level `_parse_json_response(raw: str) -> dict` helper: strip markdown fences (`re.sub`), `json.loads`, raise `ValueError` on failure.
- Module-level `_DEFAULT_PLAN` dict with all empty lists, `limit: None`, `complexity: "simple"`.
- `async def query_planner_node(state: AgentState) -> dict`:
  1. Extract `query = state.get("resolved_query") or state.get("user_query", "")`.
  2. Extract `schema = state.get("schema", {})`, `relevant_tables = state.get("relevant_tables", [])`.
  3. Format schema context as string for prompt (table names, columns, types, FK info).
  4. Lazy import ChatGroq. Instantiate with `model="llama-3.3-70b-versatile"`, `temperature=0`, `max_tokens=1024`.
  5. Build messages: SystemMessage with _PLANNER_PROMPT formatted with schema_context and relevant_tables, HumanMessage with query.
  6. `response = await llm.ainvoke(messages)`.
  7. Parse JSON from `response.content`. On ValueError, log warning and use `_DEFAULT_PLAN`.
  8. Return `{"query_plan": plan}`.

Use `ChatGroq(model="llama-3.3-70b-versatile", temperature=0, max_tokens=1024)`.
  </action>
  <verify>
    <automated>cd "F:/Agentic AI/Text-2-SQL-Agentic-Pipeline" && python -m pytest tests/agents/test_query_planner.py -v --tb=short</automated>
  </verify>
  <done>5 query planner tests pass. Planner generates structured JSON plans with all required keys, handles malformed JSON gracefully, strips markdown fences. Module-level prompt constant satisfies LLM-003.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Implement SQL generator node with tests</name>
  <files>tests/agents/test_sql_generator.py, agents/nodes/sql_generator.py</files>
  <behavior>
    - test_returns_select: Mock ainvoke returns `"SELECT SUM(Total) FROM Invoice"`. Assert result `generated_sql` starts with "SELECT".
    - test_returns_with_cte: Mock ainvoke returns `"WITH monthly AS (...) SELECT ..."`. Assert `generated_sql` starts with "WITH".
    - test_strips_fences: Mock ainvoke returns ````sql\nSELECT * FROM Invoice\n` ``` `. Assert `generated_sql` is clean SQL without fences.
    - test_rejects_non_select: Mock ainvoke returns `"DROP TABLE Invoice"`. Assert `generated_sql` is None or empty, and `error_log` is set with validation error message.
    - test_includes_explanation: Mock ainvoke returns `"SQL: SELECT ...\nEXPLANATION: This query sums..."`. Assert result contains an explanation key or the explanation is embedded in a recognizable way.
    - test_dialect_reminders: State has `db_type="postgres"`. Inspect the messages passed to LLM (via mock call args). Assert the prompt content includes dialect-specific reminder (e.g., "ILIKE" or "TO_CHAR").
    - test_prompt_constant_exists: Assert `_GENERATOR_PROMPT` (or similar) is a non-empty string (LLM-003).
  </behavior>
  <action>
**Tests first (RED):** Create `tests/agents/test_sql_generator.py` with 7 tests. Use `patch("agents.nodes.sql_generator.ChatGroq")`, `AsyncMock`, `asyncio.run()`. Import `make_agent_state`.

**Implementation (GREEN):** Replace `agents/nodes/sql_generator.py` placeholder:
- Module-level `_GENERATOR_PROMPT` constant (LLM-003). Prompt instructs LLM to: take query plan JSON + schema context + db_type, generate dialect-specific SQL with brief inline comments for non-obvious logic, provide a 1-2 sentence explanation. Output format: `SQL:\n<the sql>\nEXPLANATION:\n<1-2 sentences>`. Tell LLM to ONLY generate SELECT or WITH statements.
- Module-level `_DIALECT_REMINDERS` dict mapping db_type to dialect-specific hints:
  - `"postgres"`: "Use ILIKE for case-insensitive. Use TO_CHAR() for dates. Use || for concatenation."
  - `"mysql"`: "Use DATE_FORMAT() for dates. Use CONCAT() for concatenation. Use LIKE (case-insensitive by default)."
  - `"sqlite"`: "Use strftime() for dates. Use || for concatenation. No ILIKE — use LOWER()."
  - `"duckdb"`: "Use strptime() for date parsing. Supports QUALIFY for window filtering. Use ILIKE."
- Module-level `_validate_sql(sql: str) -> str` helper: strip markdown fences (```sql and ```), verify starts with SELECT or WITH (case-insensitive), raise ValueError otherwise.
- `async def sql_generator_node(state: AgentState) -> dict`:
  1. Extract `query = state.get("resolved_query") or state.get("user_query", "")`.
  2. Extract `query_plan = state.get("query_plan", {})`, `db_type = state.get("db_type", "sqlite")`, `schema = state.get("schema", {})`.
  3. Get dialect reminder from `_DIALECT_REMINDERS.get(db_type, "Generate standard SQL.")`.
  4. Lazy import ChatGroq. Instantiate with `model="llama-3.3-70b-versatile"`, `temperature=0`, `max_tokens=2048`.
  5. Build messages: SystemMessage with _GENERATOR_PROMPT formatted with query_plan JSON string, schema context, dialect reminder, db_type. HumanMessage with query.
  6. `response = await llm.ainvoke(messages)`.
  7. Parse response: split on "EXPLANATION:" to separate SQL and explanation parts.
  8. Validate SQL with `_validate_sql()`. On ValueError: log warning, return `{"generated_sql": None, "error_log": str(error)}`.
  9. Return `{"generated_sql": validated_sql, "sql_explanation": explanation.strip() if explanation else None}`.
  </action>
  <verify>
    <automated>cd "F:/Agentic AI/Text-2-SQL-Agentic-Pipeline" && python -m pytest tests/agents/test_sql_generator.py -v --tb=short && python -m pytest tests/ -v --tb=short</automated>
  </verify>
  <done>7 SQL generator tests pass. Generator produces dialect-specific SQL, strips fences, validates SELECT/WITH, rejects destructive SQL, includes explanation. Full test suite (88+ prior + 12 new = ~100 tests) passes with 0 failures. Module-level prompt constant satisfies LLM-003.</done>
</task>

</tasks>

<verification>
- `pytest tests/agents/test_query_planner.py -v` — 5 planner tests pass
- `pytest tests/agents/test_sql_generator.py -v` — 7 generator tests pass
- `pytest tests/ -v --tb=short` — full suite green (~100 tests, 0 failures)
- All 4 agent node files (gatekeeper, schema_linker, query_planner, sql_generator) contain real LLM-powered logic, not placeholder stubs
</verification>

<success_criteria>
- query_planner_node generates structured JSON plan with all 9 keys (select/from/joins/where/group_by/order_by/limit/ctes/complexity)
- query_planner_node handles malformed JSON gracefully with safe default plan
- sql_generator_node produces dialect-specific SQL with inline comments
- sql_generator_node validates SQL starts with SELECT/WITH, rejects other statements
- sql_generator_node returns human-readable explanation
- Module-level prompt constants in both files (LLM-003)
- Full test suite passes with zero regressions
</success_criteria>

<output>
After completion, create `.planning/phases/04-specialized-agent-nodes/04-02-SUMMARY.md`
</output>
