---
phase: 04-specialized-agent-nodes
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - graph/state.py
  - graph/conditions.py
  - agents/nodes/gatekeeper.py
  - agents/nodes/schema_linker.py
  - tests/graph/test_state.py
  - tests/agents/__init__.py
  - tests/agents/conftest.py
  - tests/agents/test_gatekeeper.py
  - tests/agents/test_schema_linker.py
autonomous: true
requirements: [AGENT-001, AGENT-002, LLM-003]
must_haves:
  truths:
    - "Gatekeeper classifies sql queries and routes to schema_linker"
    - "Gatekeeper classifies conversational queries and sets final_answer, routes to formatter"
    - "Gatekeeper detects follow-up queries and writes resolved_query without overwriting user_query"
    - "Gatekeeper returns ambiguous with clarification message, routes to formatter"
    - "Gatekeeper returns early when db_manager is None with connect message"
    - "Gatekeeper blocks natural language destructive intent"
    - "Schema linker calls retriever and populates relevant_tables and narrowed schema"
    - "Schema linker falls back to full schema when retriever fails"
    - "route_after_gatekeeper handles all 4 categories correctly"
  artifacts:
    - path: "graph/state.py"
      provides: "AgentState with resolved_query field"
      contains: "resolved_query"
    - path: "graph/conditions.py"
      provides: "Expanded routing for 4 gatekeeper categories"
      contains: "ambiguous"
    - path: "agents/nodes/gatekeeper.py"
      provides: "LLM-powered gatekeeper with classification, follow-up rewrite, NL safety"
      min_lines: 60
    - path: "agents/nodes/schema_linker.py"
      provides: "Vector retriever integration with full-schema fallback"
      min_lines: 30
    - path: "tests/agents/test_gatekeeper.py"
      provides: "6 AGENT-001 unit tests"
      min_lines: 50
    - path: "tests/agents/test_schema_linker.py"
      provides: "2 AGENT-002 unit tests"
      min_lines: 20
  key_links:
    - from: "agents/nodes/gatekeeper.py"
      to: "langchain_groq.ChatGroq"
      via: "lazy import + ainvoke"
      pattern: "ChatGroq"
    - from: "agents/nodes/schema_linker.py"
      to: "vector/retriever.py"
      via: "get_retriever() + retrieve_tables()"
      pattern: "get_retriever"
    - from: "graph/conditions.py"
      to: "graph/state.py"
      via: "reads query_type field"
      pattern: "query_type.*ambiguous"
---

<objective>
Implement the gatekeeper and schema linker agent nodes with supporting state/routing changes and full test coverage.

Purpose: Replace placeholder stubs with real LLM-powered gatekeeper (4-category classification, follow-up rewrite, NL safety) and vector-retriever-backed schema linker. These are the "input side" of the pipeline — they classify the query and retrieve relevant schema before planning/generation.

Output: Working gatekeeper_node, schema_linker_node, expanded AgentState (resolved_query), updated routing, 8+ unit tests all passing.
</objective>

<execution_context>
@C:/Users/Chaitanya/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/Chaitanya/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-specialized-agent-nodes/04-CONTEXT.md
@.planning/phases/04-specialized-agent-nodes/04-RESEARCH.md

<interfaces>
<!-- Key types and contracts the executor needs. Extracted from codebase. -->

From graph/state.py (CURRENT — will be modified):
```python
class AgentState(TypedDict):
    messages: Annotated[list, add_messages]
    user_query: str
    db_type: str
    db_manager: Optional[object]
    query_type: Optional[str]
    schema: Optional[dict]
    relevant_tables: Optional[list]
    query_plan: Optional[str]
    generated_sql: Optional[str]
    db_results: Optional[list]
    error_log: Optional[str]
    retry_count: int
    final_answer: Optional[str]
```

From graph/conditions.py (CURRENT — will be modified):
```python
def route_after_gatekeeper(state: AgentState) -> str:
    if state.get("query_type") == "conversational":
        return "formatter"
    return "schema_linker"
```

From graph/builder.py (NO changes needed — path map already covers both destinations):
```python
wf.add_conditional_edges(
    "gatekeeper",
    route_after_gatekeeper,
    {"schema_linker": "schema_linker", "formatter": "formatter"},
)
```

From vector/retriever.py (consumed by schema_linker):
```python
def get_retriever() -> BaseRetriever:  # factory, reads PINECONE_API_KEY env var
class BaseRetriever(ABC):
    def embed_schema(self, schema: dict, namespace: str) -> None: ...
    def retrieve_tables(self, query: str, namespace: str, top_k: int = 5) -> dict:
        # returns {"tables": [...], "table_metadata": {...}, "join_hints": [...], "scores": {...}}
```

From tests/graph/test_state.py (CURRENT — will be modified):
```python
EXPECTED_FIELDS = { "messages", "user_query", "db_type", ... }  # 13 fields
assert len(hints) == 13
```

From agents/nodes/gatekeeper.py (CURRENT placeholder):
```python
async def gatekeeper_node(state: AgentState) -> dict:
    logger.info("gatekeeper_node called for query: %s", state.get("user_query", ""))
    return {}
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Expand AgentState, update routing, create test infrastructure</name>
  <files>graph/state.py, graph/conditions.py, tests/graph/test_state.py, tests/agents/__init__.py, tests/agents/conftest.py</files>
  <action>
1. **graph/state.py** — Add `resolved_query: Optional[str]` field to AgentState TypedDict. Place it after `user_query`. This brings the field count from 13 to 14.

2. **graph/conditions.py** — Update `route_after_gatekeeper` to handle 4 categories. The function must ONLY return values that exist in builder.py's path map (`"schema_linker"` or `"formatter"`). Logic:
   - `query_type in ("conversational", "ambiguous")` → return `"formatter"`
   - Everything else (including `"sql"`, `"follow_up"`, and `None`) → return `"schema_linker"`
   Do NOT modify `route_after_executor` — it is unchanged.

3. **tests/graph/test_state.py** — Update `EXPECTED_FIELDS` set to include `"resolved_query"`. Update the field count assertion from `== 13` to `== 14`. Add test cases to `test_routing_gatekeeper` for `"follow_up"` → `"schema_linker"` and `"ambiguous"` → `"formatter"`.

4. **tests/agents/__init__.py** — Create empty package marker file.

5. **tests/agents/conftest.py** — Create `make_agent_state(user_query, db_manager)` helper function (plain function, not fixture) that returns a complete AgentState dict with all 14 fields. Default `db_manager=object()` (non-None = connected), `resolved_query=None`, `db_type="sqlite"`, `schema={"Invoice": {"columns": [{"name": "Total", "type": "NUMERIC"}]}}`. Follow the pattern established in `tests/graph/conftest.py::make_initial_state`.
  </action>
  <verify>
    <automated>cd "F:/Agentic AI/Text-2-SQL-Agentic-Pipeline" && python -m pytest tests/graph/test_state.py -v --tb=short</automated>
  </verify>
  <done>AgentState has 14 fields including resolved_query. route_after_gatekeeper handles sql/conversational/follow_up/ambiguous. All 5 existing graph/test_state tests pass with updated assertions. tests/agents/conftest.py provides make_agent_state helper.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Implement gatekeeper node with tests</name>
  <files>tests/agents/test_gatekeeper.py, agents/nodes/gatekeeper.py</files>
  <behavior>
    - test_classifies_sql: Mock ChatGroq.ainvoke returns `{"category":"sql","intent":"user wants total sales","response":""}`. Assert result has `query_type="sql"`, `intent` key present.
    - test_classifies_conversational: Mock returns `{"category":"conversational","intent":"greeting","response":"Hello! How can I help?"}`. Assert `query_type="conversational"`, `final_answer` contains the response.
    - test_follow_up_rewrite: Mock returns `{"category":"follow_up","intent":"filter by region","response":""}`. Then a SECOND ainvoke call returns the rewritten standalone query. Assert `resolved_query` is set, `user_query` is NOT in result (preserved).
    - test_ambiguous_clarification: Mock returns `{"category":"ambiguous","intent":"unclear","response":"Could you clarify..."}`. Assert `query_type="ambiguous"`, `final_answer` contains clarification.
    - test_no_db_manager: Pass state with `db_manager=None`. Assert returns `final_answer` containing "connect to a database", `query_type="conversational"`. No LLM call made.
    - test_blocks_destructive: Query is "delete all users from the database". Mock returns `{"category":"sql","intent":"delete users","response":""}`. Gatekeeper detects destructive NL intent BEFORE or AFTER classification. Assert `final_answer` contains "read-only", `query_type="conversational"`.
    - test_prompt_constant_exists: Assert `_GATEKEEPER_PROMPT` (or similar module-level constant) is a non-empty string (LLM-003).
  </behavior>
  <action>
**Tests first (RED):** Create `tests/agents/test_gatekeeper.py` with 7 tests using `asyncio.run()`, `MagicMock`, `AsyncMock`, and `patch("agents.nodes.gatekeeper.ChatGroq")`. Import `make_agent_state` from `tests.agents.conftest`. Each test constructs a mock LLM, patches ChatGroq constructor to return it, and asserts on returned state dict keys.

**Implementation (GREEN):** Replace `agents/nodes/gatekeeper.py` placeholder with real logic:
- Module-level `_GATEKEEPER_PROMPT` string constant (LLM-003). Prompt instructs LLM to classify into sql/conversational/follow_up/ambiguous, extract intent, and provide response. Include JSON output format instructions.
- Module-level `_DESTRUCTIVE_PATTERNS` list: `["delete", "drop", "truncate", "remove all", "destroy", "erase"]` for NL safety check.
- `async def gatekeeper_node(state: AgentState) -> dict`:
  1. DB connection check: if `state.get("db_manager")` is None, return `{"final_answer": "Please connect to a database first.", "query_type": "conversational"}` immediately. No LLM call.
  2. NL safety check: scan `user_query.lower()` for destructive patterns. If found, return `{"final_answer": "I can only run read-only queries. I cannot delete or modify data.", "query_type": "conversational"}`.
  3. Lazy import `ChatGroq` from `langchain_groq` inside function body.
  4. Build messages with SystemMessage(_GATEKEEPER_PROMPT formatted with chat_history and user_query) + HumanMessage.
  5. `response = await llm.ainvoke(messages)`
  6. Parse JSON from `response.content` using `_parse_json_response` helper (strip markdown fences, json.loads, catch ValueError → default to sql category).
  7. Extract `category`, `intent`, `response_text`.
  8. If `category == "follow_up"`: make a second LLM call to rewrite the follow-up as a standalone query. Store in `resolved_query`.
  9. If `category in ("conversational", "ambiguous")`: set `final_answer` from response_text.
  10. Return partial state dict: `{"query_type": category, ...}` plus optional `intent`, `resolved_query`, `final_answer`.

Use `ChatGroq(model="llama-3.3-70b-versatile", temperature=0, max_tokens=1024)`.
  </action>
  <verify>
    <automated>cd "F:/Agentic AI/Text-2-SQL-Agentic-Pipeline" && python -m pytest tests/agents/test_gatekeeper.py -v --tb=short</automated>
  </verify>
  <done>7 gatekeeper tests pass. Gatekeeper classifies into 4 categories, rewrites follow-ups to resolved_query, blocks destructive NL intent, returns early on missing db_manager. Module-level prompt constant satisfies LLM-003.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Implement schema linker node with tests</name>
  <files>tests/agents/test_schema_linker.py, agents/nodes/schema_linker.py</files>
  <behavior>
    - test_retriever_populates_tables: Mock `get_retriever()` returns a mock retriever. `retrieve_tables()` returns `{"tables": ["Invoice","Customer"], "table_metadata": {}, "join_hints": [], "scores": {}}`. State has `schema={"Invoice": {...}, "Customer": {...}, "Track": {...}}`. Assert result `relevant_tables == ["Invoice","Customer"]`, result `schema` only contains Invoice and Customer keys (narrowed).
    - test_fallback_full_schema: Mock `get_retriever()` raises Exception. Assert result `relevant_tables` == all keys from original schema, result `schema` == original full schema.
    - test_uses_resolved_query: State has `resolved_query="total sales last year"` and `user_query="what about last year?"`. Mock retriever. Assert retriever.retrieve_tables was called with the resolved_query, not user_query.
  </behavior>
  <action>
**Tests first (RED):** Create `tests/agents/test_schema_linker.py` with 3 tests. Use `patch("agents.nodes.schema_linker.get_retriever")` to mock the retriever factory. Use `asyncio.run()` for async execution. Import `make_agent_state` from conftest.

**Implementation (GREEN):** Replace `agents/nodes/schema_linker.py` placeholder with real logic:
- `async def schema_linker_node(state: AgentState) -> dict`:
  1. Extract query: `state.get("resolved_query") or state.get("user_query", "")`.
  2. Extract `db_type = state.get("db_type", "unknown")` and `full_schema = state.get("schema")`.
  3. Try block: lazy import `get_retriever` from `vector.retriever`. Call `retriever = get_retriever()`. If `full_schema`, call `retriever.embed_schema(full_schema, db_type)` (idempotent). Call `result = retriever.retrieve_tables(query, db_type, top_k=5)`.
  4. Extract `relevant_tables = result["tables"]`. Narrow schema: `{t: full_schema[t] for t in relevant_tables if full_schema and t in full_schema}`. If narrowed is empty, use full_schema.
  5. Return `{"relevant_tables": relevant_tables, "schema": narrowed_schema}`.
  6. Except block: log warning, return `{"relevant_tables": list(full_schema.keys()) if full_schema else [], "schema": full_schema}`.

No LLM call in this node. No prompt constant needed.
  </action>
  <verify>
    <automated>cd "F:/Agentic AI/Text-2-SQL-Agentic-Pipeline" && python -m pytest tests/agents/test_schema_linker.py -v --tb=short && python -m pytest tests/ -v --tb=short</automated>
  </verify>
  <done>3 schema linker tests pass. Schema linker calls retriever, narrows schema to relevant tables, falls back to full schema on failure. Full test suite (73 prior + new tests) passes with 0 failures.</done>
</task>

</tasks>

<verification>
- `pytest tests/graph/test_state.py -v` — all 5 state tests pass (updated for 14 fields + new routing categories)
- `pytest tests/agents/test_gatekeeper.py -v` — 7 gatekeeper tests pass
- `pytest tests/agents/test_schema_linker.py -v` — 3 schema linker tests pass
- `pytest tests/ -v --tb=short` — full suite green (73 prior + ~15 new = ~88 tests, 0 failures)
</verification>

<success_criteria>
- AgentState has 14 fields including resolved_query
- route_after_gatekeeper handles sql, conversational, follow_up, ambiguous — returns only "schema_linker" or "formatter"
- gatekeeper_node classifies queries via ChatGroq, rewrites follow-ups, blocks destructive NL, checks db_manager
- schema_linker_node calls get_retriever, narrows schema, falls back gracefully
- All tests pass including prior 73 tests with zero regressions
- Module-level prompt constant in gatekeeper.py (LLM-003)
</success_criteria>

<output>
After completion, create `.planning/phases/04-specialized-agent-nodes/04-01-SUMMARY.md`
</output>
