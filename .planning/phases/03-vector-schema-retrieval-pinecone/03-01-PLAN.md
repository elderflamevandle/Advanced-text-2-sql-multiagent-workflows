---
phase: 03-vector-schema-retrieval-pinecone
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - pyproject.toml
  - vector/embeddings.py
  - vector/schema_graph.py
  - tests/vector/__init__.py
  - tests/vector/conftest.py
  - tests/vector/test_embeddings.py
  - tests/vector/test_schema_graph.py
autonomous: true
requirements:
  - VECTOR-002
  - VECTOR-003

must_haves:
  truths:
    - "pyproject.toml declares pinecone>=5.1.0 (not pinecone-client) and vector optional extras group"
    - "EmbeddingGenerator lazily loads BAAI/bge-large-en-v1.5 model on first call"
    - "embed_query() applies BGE instruction prefix via encode_query() with fallback"
    - "embed_documents() batches in chunks of 50 with progress logging"
    - "embed_query_cached() returns tuple and caches via lru_cache(maxsize=10_000)"
    - "build_table_text() formats SchemaTable into embedding text per locked template"
    - "build_column_text() formats column info per locked template"
    - "SchemaGraph builds adjacency dict from FKInfo data"
    - "expand_tables() adds 1-hop FK neighbors only"
    - "generate_join_hints() produces from/to/on/type dicts"
  artifacts:
    - path: "pyproject.toml"
      provides: "Updated dependencies — pinecone replaces pinecone-client, vector extras added"
      contains: "pinecone>=5.1.0"
    - path: "vector/embeddings.py"
      provides: "EmbeddingGenerator class + text builders"
      exports: ["EmbeddingGenerator", "build_table_text", "build_column_text"]
      min_lines: 80
    - path: "vector/schema_graph.py"
      provides: "FK adjacency graph + JOIN hint generation"
      exports: ["SchemaGraph"]
      min_lines: 40
    - path: "tests/vector/test_embeddings.py"
      provides: "Unit tests for VECTOR-002 behaviors"
      min_lines: 60
    - path: "tests/vector/test_schema_graph.py"
      provides: "Unit tests for FK expansion and JOIN hints (VECTOR-003)"
      min_lines: 50
  key_links:
    - from: "vector/embeddings.py"
      to: "database/schema_utils.py"
      via: "imports SchemaTable, ColumnInfo, FKInfo for build_table_text/build_column_text"
      pattern: "from database.schema_utils import"
    - from: "vector/schema_graph.py"
      to: "database/schema_utils.py"
      via: "imports SchemaTable, FKInfo for adjacency graph construction"
      pattern: "from database.schema_utils import"
    - from: "tests/vector/test_schema_graph.py"
      to: "vector/schema_graph.py"
      via: "imports SchemaGraph to test expand_tables and generate_join_hints"
      pattern: "from vector.schema_graph import SchemaGraph"
---

<objective>
Create the embedding generation layer and FK schema graph for semantic schema retrieval.

Purpose: Provides the core embedding and graph building primitives that the retrievers (Plan 02) depend on. Fixes the critical pyproject.toml dependency issue (pinecone-client is deprecated) and adds the vector optional extras group. Implements SchemaGraph for 1-hop FK expansion and JOIN hint generation.

Output: `vector/embeddings.py` (EmbeddingGenerator + text builders), `vector/schema_graph.py` (SchemaGraph), test files, updated `pyproject.toml`
</objective>

<execution_context>
@C:/Users/Chaitanya/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/Chaitanya/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-vector-schema-retrieval-pinecone/03-CONTEXT.md
@.planning/phases/03-vector-schema-retrieval-pinecone/03-RESEARCH.md

<interfaces>
<!-- Key types and contracts the executor needs. Extracted from codebase. -->

From database/schema_utils.py:
```python
class ColumnInfo(TypedDict):
    name: str
    type: str
    nullable: bool

class FKInfo(TypedDict):
    column: str
    references_table: str
    references_column: str

class SchemaTable(TypedDict):
    columns: list[ColumnInfo]
    primary_keys: list[str]
    foreign_keys: list[FKInfo]
    sample_rows: list[dict]
```

From graph/state.py:
```python
class AgentState(TypedDict):
    # ...
    schema: Optional[dict]           # dict[str, SchemaTable] at runtime
    relevant_tables: Optional[list]  # retrieval results land here
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Fix pyproject.toml dependencies and create test scaffold</name>
  <files>pyproject.toml, tests/vector/__init__.py, tests/vector/conftest.py</files>
  <action>
1. Update pyproject.toml:
   - Remove `"pinecone-client>=3.0.0,<5.0.0"` from `[project] dependencies`
   - Add a new optional extras group:
     ```
     vector = [
         "pinecone>=5.1.0",
         "sentence-transformers>=5.0.0,<6.0.0",
         "chromadb>=0.5.0",
     ]
     ```
   - Keep all other dependencies and extras groups unchanged
   - Do NOT add pinecone to core dependencies — it belongs in the vector extras only

2. Create `tests/vector/__init__.py` as an empty package marker file (just a docstring: `"""Vector retrieval tests."""`)

3. Create `tests/vector/conftest.py` with shared test fixtures:
   - `sample_schema` fixture: returns a `dict[str, SchemaTable]` with 3 tables mimicking Chinook (e.g., Invoice, Customer, InvoiceLine) including realistic columns, primary_keys, foreign_keys (Invoice.CustomerId -> Customer.CustomerId, InvoiceLine.InvoiceId -> Invoice.InvoiceId), and 2 sample_rows per table
   - `sample_table_name` fixture: returns `"Invoice"`
   - `sample_column_info` fixture: returns a `ColumnInfo` dict `{"name": "Total", "type": "NUMERIC", "nullable": False}`
   - All fixtures use `@pytest.fixture` decorator
   - Import SchemaTable, ColumnInfo, FKInfo from database.schema_utils
  </action>
  <verify>
    <automated>cd "F:/Agentic AI/Text-2-SQL-Agentic-Pipeline" && python -c "import tomllib; d=tomllib.load(open('pyproject.toml','rb')); assert 'pinecone-client' not in str(d['project']['dependencies']), 'pinecone-client still in deps'; assert 'pinecone>=5.1.0' in d['project']['optional-dependencies']['vector'], 'pinecone not in vector extras'; print('pyproject.toml OK')" && python -c "import tests.vector; print('test package OK')"</automated>
  </verify>
  <done>pyproject.toml has pinecone-client removed from core deps, vector extras group added with pinecone>=5.1.0 + sentence-transformers + chromadb. tests/vector/ package exists with conftest.py providing sample_schema fixture.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Implement EmbeddingGenerator and text builders</name>
  <files>vector/embeddings.py, tests/vector/test_embeddings.py</files>
  <behavior>
    - test_build_table_text_format: build_table_text("Invoice", schema["Invoice"]) returns string containing "Table: Invoice.", "Columns:", column names with types, "Foreign keys:", FK references
    - test_build_table_text_no_fks: build_table_text for a table with empty foreign_keys list omits "Foreign keys:" section
    - test_build_table_text_no_samples: build_table_text for a table with empty sample_rows omits "Sample:" section
    - test_build_column_text_format: build_column_text("Total", "NUMERIC", "Invoice", [100.0, 200.5]) returns "Column: Total (NUMERIC) in table Invoice. Sample values: 100.0, 200.5"
    - test_build_column_text_max_3_samples: build_column_text with 5 sample values only includes first 3
    - test_embedding_generator_lazy_load: EmbeddingGenerator()._model is None before any call
    - test_embed_query_cached_returns_tuple: embed_query_cached("test query") returns a tuple (not list/ndarray)
    - test_embed_query_cached_cache_hit: calling embed_query_cached with same string twice only calls embed_query once (mock embed_query)
    - test_embed_documents_batching: embed_documents with 120 texts calls model.encode 3 times (chunks of 50, 50, 20) — mock the model
    - test_embed_documents_progress_logging: embed_documents with >50 texts emits info log messages with progress counts
  </behavior>
  <action>
Create `vector/embeddings.py` with:

1. Module-level constant: `QUERY_INSTRUCTION = "Represent this sentence for searching relevant passages: "`

2. `build_table_text(name: str, table: SchemaTable) -> str` function:
   - Format: `"Table: {name}. Columns: {col1} ({type}), {col2} ({type}), ... Foreign keys: {fk_col} references {ref_table}.{ref_col}. Sample: {row1_vals}"`
   - Omit "Foreign keys:" section if table["foreign_keys"] is empty
   - Omit "Sample:" section if table["sample_rows"] is empty
   - Use first sample row only: `str(table["sample_rows"][0])`
   - Import SchemaTable from database.schema_utils

3. `build_column_text(col_name: str, col_type: str, table_name: str, sample_values: list) -> str` function:
   - Format: `"Column: {col_name} ({col_type}) in table {table_name}. Sample values: {val1}, {val2}"`
   - Limit to first 3 sample values
   - Handle empty sample_values gracefully (omit "Sample values:" part or show empty)

4. `EmbeddingGenerator` class:
   - `MODEL_NAME = "BAAI/bge-large-en-v1.5"` class attribute
   - `__init__`: set `self._model = None` (lazy load)
   - `_get_model() -> SentenceTransformer`: lazy-load the model on first call. Log info message before loading: `"Loading BGE model (first run may download ~600MB)..."`. Import `SentenceTransformer` lazily inside this method (not at module top level) to follow the project's lazy import pattern for optional extras.
   - `embed_query(query: str) -> list[float]`: get model, try `model.encode_query(query, normalize_embeddings=True)`, fallback to `model.encode(QUERY_INSTRUCTION + query, normalize_embeddings=True)` on AttributeError. Return `.tolist()`
   - `embed_query_cached(query: str) -> tuple[float, ...]`: decorated with `@lru_cache(maxsize=10_000)`. Calls `self.embed_query(query)` and returns `tuple(result)`. NOTE: `lru_cache` on a method requires the instance to be hashable — add `__hash__` returning `id(self)` and `__eq__` checking `self is other` to EmbeddingGenerator.
   - `embed_documents(texts: list[str], batch_size: int = 50) -> list[list[float]]`: get model, process in chunks of `batch_size`. For each chunk, log `"Embedding %d/%d..."` with progress. Use `model.encode(chunk, batch_size=batch_size, show_progress_bar=False, normalize_embeddings=True, convert_to_numpy=True)`. Return list of `.tolist()` vectors. Set `show_progress_bar=False` since we do our own progress logging.

5. Create `tests/vector/test_embeddings.py` — write tests FIRST per TDD:
   - Import `build_table_text`, `build_column_text`, `EmbeddingGenerator` from `vector.embeddings`
   - Use `sample_schema` fixture from conftest for table text tests
   - Mock `SentenceTransformer` for embedding tests — avoid downloading the real model in tests
   - For `test_embed_documents_batching`: create a mock model whose `encode()` returns a numpy array of shape `(len(chunk), 1024)`. Patch `EmbeddingGenerator._get_model` to return the mock. Call `embed_documents` with 120 dummy texts and assert `mock_model.encode.call_count == 3`
   - For `test_embed_query_cached_cache_hit`: patch `embed_query` on the EmbeddingGenerator instance, call `embed_query_cached("same query")` twice, assert `embed_query` was called only once
  </action>
  <verify>
    <automated>cd "F:/Agentic AI/Text-2-SQL-Agentic-Pipeline" && python -m pytest tests/vector/test_embeddings.py -x -v</automated>
  </verify>
  <done>All 10 embedding tests pass. build_table_text and build_column_text format schema data per locked templates. EmbeddingGenerator lazily loads BGE model, caches query embeddings, and batches document embeddings in chunks of 50.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Implement SchemaGraph with FK expansion and JOIN hints</name>
  <files>vector/schema_graph.py, tests/vector/test_schema_graph.py</files>
  <behavior>
    - test_adjacency_built_from_fkinfo: SchemaGraph constructed from sample_schema has adjacency entries for Invoice (FK to Customer) and InvoiceLine (FK to Invoice)
    - test_expand_tables_1hop: expand_tables(["Invoice"]) returns set containing Invoice + Customer (1-hop neighbor via FK)
    - test_expand_tables_no_further: expand_tables(["Customer"]) returns only ["Customer"] if Customer has no outgoing FKs
    - test_expand_tables_multiple_seeds: expand_tables(["Invoice", "InvoiceLine"]) returns Invoice, Customer, InvoiceLine (InvoiceLine FK to Invoice already in seed, Invoice FK to Customer added)
    - test_join_hints_format: generate_join_hints(["Invoice"]) returns list with dict {"from": "Invoice", "to": "Customer", "on": "Invoice.CustomerId = Customer.CustomerId", "type": "INNER"}
    - test_join_hints_multiple_fks: generate_join_hints(["InvoiceLine"]) returns hint for InvoiceLine -> Invoice FK
    - test_join_hints_empty_for_no_fk_table: generate_join_hints(["Customer"]) returns empty list when Customer has no outgoing FKs
    - test_schema_graph_empty_schema: SchemaGraph({}) has empty adjacency, expand_tables([]) returns []
  </behavior>
  <action>
Create `vector/schema_graph.py` with:

1. `SchemaGraph` class:
   - Import `SchemaTable`, `FKInfo` from `database.schema_utils`
   - `__init__(self, schema: dict[str, SchemaTable])`: build `self._adj: dict[str, list[tuple[str, str, str]]]` — adjacency dict where key is table_name, value is list of `(fk_column, references_table, references_column)` tuples. Iterate all tables, iterate their `foreign_keys` list, populate adjacency. Also build reverse adjacency `self._rev_adj` for tables referenced BY other tables (needed so expand catches both directions — but per locked decision, only outgoing FKs are followed, so we only need `_adj` for forward edges)
   - `expand_tables(self, seed_tables: list[str]) -> list[str]`: start with `expanded = set(seed_tables)`. For each table in seed_tables, add all `ref_table` values from `self._adj.get(table, [])`. Also check reverse: for each table in seed_tables, check if any OTHER table has an FK pointing TO this table — add those too. This ensures "if Invoice is seed and InvoiceLine has FK to Invoice, InvoiceLine gets added." Wait — the locked decision says "1-hop only, expand to directly FK-linked tables." The example says "If Invoice is retrieved and has FK to Customer, include Customer." This is outgoing-FK-only. But we should also include reverse direction: if Invoice is retrieved, and InvoiceLine references Invoice, include InvoiceLine? The locked decision uses the phrase "FK-linked" which could mean both directions. However, the example only shows forward direction. Implement forward-only (outgoing FKs from seed tables) per the example. The reverse adjacency is a nice-to-have but not locked.
   - Actually, re-reading: "expand to directly FK-linked tables" — this means any table connected via a FK relationship, either direction. Build `_rev_adj` too: for each table having FK to ref_table, add to `_rev_adj[ref_table]` the source table. In expand_tables, for each seed, add both `_adj[seed]` targets AND `_rev_adj[seed]` sources.
   - NO — keep it simple. The locked decision example is clear: "If Invoice has FK to Customer, include Customer." Only follow outgoing FKs. This avoids context explosion (a central table like Customer could have 10 tables pointing to it).
   - `expand_tables`: forward-only. `expanded = set(seed_tables)`, for each seed, for each `(_, ref_table, _)` in `_adj.get(seed, [])`, add `ref_table`. Return `sorted(list(expanded))` for deterministic output.
   - `generate_join_hints(self, tables: list[str]) -> list[dict]`: for each table in `tables`, for each `(fk_col, ref_table, ref_col)` in `_adj.get(table, [])`, append `{"from": table, "to": ref_table, "on": f"{table}.{fk_col} = {ref_table}.{ref_col}", "type": "INNER"}`. Return the list.

2. Create `tests/vector/test_schema_graph.py` — write tests FIRST per TDD:
   - Import `SchemaGraph` from `vector.schema_graph`
   - Use `sample_schema` fixture from conftest
   - Test adjacency construction, expand_tables (1-hop forward only), generate_join_hints format, empty schema edge case
  </action>
  <verify>
    <automated>cd "F:/Agentic AI/Text-2-SQL-Agentic-Pipeline" && python -m pytest tests/vector/test_schema_graph.py -x -v</automated>
  </verify>
  <done>All 8 SchemaGraph tests pass. SchemaGraph builds adjacency from FKInfo, expand_tables adds 1-hop FK neighbors (forward direction only), generate_join_hints produces correct from/to/on/type dicts. Empty schema handled gracefully.</done>
</task>

</tasks>

<verification>
After all tasks complete:
1. `python -m pytest tests/vector/ -x -v` — all 18+ vector tests pass
2. `python -m pytest tests/ -v` — full suite green (41 prior + new vector tests, 0 failures)
3. `python -c "import tomllib; d=tomllib.load(open('pyproject.toml','rb')); assert 'pinecone-client' not in str(d['project']['dependencies'])"` — pinecone-client removed
4. `python -c "from vector.embeddings import EmbeddingGenerator, build_table_text, build_column_text; print('imports OK')"` — embeddings module importable
5. `python -c "from vector.schema_graph import SchemaGraph; print('imports OK')"` — schema_graph module importable
</verification>

<success_criteria>
- pyproject.toml has pinecone>=5.1.0 in vector extras, pinecone-client removed from core deps
- vector/embeddings.py exports EmbeddingGenerator (lazy BGE model, lru_cache queries, batch documents), build_table_text, build_column_text
- vector/schema_graph.py exports SchemaGraph (adjacency from FKInfo, expand_tables 1-hop, generate_join_hints)
- All vector tests pass, full test suite remains green (41+ prior tests unbroken)
</success_criteria>

<output>
After completion, create `.planning/phases/03-vector-schema-retrieval-pinecone/03-01-SUMMARY.md`
</output>
